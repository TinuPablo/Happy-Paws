# Progress Log — Happy Paws

## 1 — Análisis de contexto.md y sub-paso A de 01a-eliminar-mascota-virtual
- Qué se hizo: Se leyó y analizó `documents/mds/Fupa nuevo/contexto.md` (visión completa del producto de adopción). Luego se leyó `01a-eliminar-mascota-virtual-atomizado.md` y se ejecutó el sub-paso A (listado de estados/funciones/JSX de `app/page.tsx`, sin editar nada).
- Archivos tocados: ninguno editado; se leyeron `documents/mds/Fupa nuevo/contexto.md`, `documents/mds/Fupa nuevo/01a-eliminar-mascota-virtual-atomizado.md`, `app/page.tsx`.
- Estado: completo. No se encontraron restos de mascota virtual (puntos, tienda, ánimo, streak, tareas diarias) en `app/page.tsx` — el archivo ya contiene solo lógica real (vacunación, recordatorios, razas, guías, perfil). El checklist de AGENTS.md ya tenía marcado "Eliminada mascota virtual", así que los sub-pasos B y C del atomizado 01a no aplican.
- Próximo paso sugerido: pasar al atomizado `02a-reestructuracion-rutas-atomizado.md`.

## 2 — Reestructuración de rutas (02a completo, sub-pasos 0 al 9)
- Qué se hizo: Se agregaron las variables CSS planas (`--brown-lightest`, etc.) a `:root` en `globals.css` (no existían, solo sus equivalentes `--color-*` de Tailwind). Se crearon las rutas nuevas `/mascotas`, `/mascotas/[id]`, `/protectoras`, `/protectoras/[id]`, `/guias`, `/(auth)/login`, `/(auth)/registro`, `/perfil`, todas con contenido placeholder según especificaba el atomizado. Se movió el contenido de guías (recomendaciones vía `getRecommendedGuides` + guías generales) desde `app/page.tsx` a `app/guias/page.tsx`, adaptando el wrapper de "pantalla dentro del mock de teléfono" a un `<main>` de página web normal, y reemplazando la dependencia del estado `petData` (que ya no existe fuera del componente viejo) por un objeto de ejemplo fijo (Labrador/perro) para no romper la lógica de recomendación. `app/page.tsx` quedó con el placeholder de landing pedido. Se agregó una barra de navegación simple en `app/layout.tsx` con links a todas las rutas nuevas. Se corrió `npm run build`: compiló sin errores y las 9 rutas esperadas están presentes.
- Archivos tocados: `app/globals.css`, `app/mascotas/page.tsx` (nuevo), `app/mascotas/[id]/page.tsx` (nuevo), `app/protectoras/page.tsx` (nuevo), `app/protectoras/[id]/page.tsx` (nuevo), `app/guias/page.tsx` (nuevo), `app/(auth)/login/page.tsx` (nuevo), `app/(auth)/registro/page.tsx` (nuevo), `app/perfil/page.tsx` (nuevo), `app/page.tsx` (reemplazado por placeholder), `app/layout.tsx` (nav agregado), `AGENTS.md` (checklist actualizado).
- Estado: completo, sin errores de compilación.
- Advertencia importante: el resto de las pantallas que vivían en el `app/page.tsx` viejo (login mock, home con resumen de vacunas, agenda de vacunación/recordatorios con notificaciones nativas, perfil con edición de usuario/mascota, términos y privacidad) **no se migraron a ninguna ruta nueva** — el atomizado 02a solo pedía mover guías y dejar el resto como placeholder. Esa funcionalidad queda huérfana (no se borró el código en sí, pero ya no es alcanzable desde ninguna ruta) hasta que un atomizado futuro decida dónde va cada pieza (probablemente `03a`/`04a` u otro paso no documentado todavía para agenda/vacunación).
- Próximo paso sugerido: leer y analizar `03a-landing-y-diseno-atomizado.md`.

## 3 — Landing y diseño visual (03a completo, sub-pasos 1 al 9)
- Qué se hizo: Se crearon `data/mock-mascotas.ts` y `data/mock-protectoras.ts` con datos temporales tipados. Se reconstruyó `app/page.tsx` con las 4 secciones pedidas (hero, mascotas destacadas, cómo funciona, protectoras participantes). Se rediseñaron `app/mascotas/page.tsx` y `app/protectoras/page.tsx` reutilizando las mismas cards que la landing, consumiendo los datos mock. `npm run build` compiló sin errores. Se verificó además, leyendo el HTML estático generado en `.next/server/app`, que `index.html`, `mascotas.html` y `protectoras.html` efectivamente contienen los nombres de las mascotas y protectoras mock (Firulais, Michi, Rocky, Luna, FUPA, Huellitas de Punilla) — no solo que compiló, sino que el contenido se renderiza.
- Archivos tocados: `data/mock-mascotas.ts` (nuevo), `data/mock-protectoras.ts` (nuevo), `app/page.tsx` (reescrito), `app/mascotas/page.tsx` (reescrito), `app/protectoras/page.tsx` (reescrito), `AGENTS.md` (checklist actualizado).
- Estado: completo, sin errores de compilación.
- Próximo paso sugerido: leer y analizar `04a-login-registro-atomizado.md`.

## 4 — Login y registro visual/mock (04a completo, sub-pasos 1 al 5)
- Qué se hizo: Se reescribió `app/(auth)/login/page.tsx` como client component con formulario de email/contraseña y submit simulado (mock marcado con comentario). Se reescribió `app/(auth)/registro/page.tsx` combinando el selector de tipo de usuario (adoptante/protectora) y ambos formularios completos en un solo archivo (los sub-pasos 2, 3 y 4 del atomizado se aplicaron juntos ya que solo el resultado final importaba). `npm run build` compiló sin errores. Además, como esta pantalla depende de interactividad real (toggle de formulario vía useState), se probó en un navegador de verdad: se levantó `npm run dev`, y se usó Playwright (paquete `playwright-core` vía npx, apuntando al Chrome ya instalado en el sistema porque no había red para descargar el Chromium propio de Playwright) para navegar a /login y /registro, hacer click en "Quiero adoptar" y "Soy una protectora", y sacar capturas de cada estado. Se confirmó visualmente que cada botón muestra su formulario correspondiente (nombre/email/teléfono/contraseña para adoptante; nombre de protectora/ubicación/email/contraseña para protectora) y que no hubo errores de consola.
- Archivos tocados: `app/(auth)/login/page.tsx` (reescrito), `app/(auth)/registro/page.tsx` (reescrito), `AGENTS.md` (checklist actualizado). Se generaron capturas temporales en el scratchpad de la sesión (no en el repo).
- Estado: completo, sin errores de compilación ni de consola. Verificado en navegador real, no solo con el build.
- Observación de diseño (no corregida, fuera de alcance de este atomizado): `app/globals.css` centra `body` con flexbox (`display:flex; justify-content:center; align-items:center`) — remanente del diseño viejo tipo "mockup de teléfono". En las páginas nuevas esto deja mucho espacio vacío a los costados en vez de ocupar el ancho completo de la pantalla (se ve en las capturas de login/registro). No lo toqué porque no estaba pedido en ningún sub-paso, pero probablemente haya que limpiarlo cuando se ajuste el layout global.
- Próximo paso sugerido: no quedan más atomizados en `documents/mds/Fupa nuevo/` (contexto, 01a, 02a, 03a y 04a completos). Definir con el usuario cuál es el siguiente paso — posiblemente decidir qué hacer con la funcionalidad huérfana de vacunación/agenda/perfil (ver entrada 2), o encarar el ajuste de `body` en `globals.css`.

## 5 — Backup del código de vacunación/agenda + shell de app de celular
- Qué se hizo: (a) Se detectó, revisando `git log`/`git diff` sobre `app/page.tsx`, que la versión "limpia" (sin mascota virtual, con vacunación/agenda) que se usó como referencia en la entrada 2 NUNCA se había commiteado — el último commit real (`62c523d`) todavía mezcla vacunación con mascota virtual (points/mood/tienda/tasks). Se guardó el contenido completo de esa versión limpia (capturado en el historial de esta conversación) en `documents/mds/Fupa nuevo/backup-page-vacunacion-agenda.tsx.txt` como referencia, ya que no es código vivo. (b) El usuario reportó que el layout se veía descentrado/roto (nav arriba y hero desplazados) en captura de pantalla, y pidió además volver al formato de "app de celular" que tenía el diseño viejo (mockup de teléfono + nav inferior) en vez de una web de escritorio. Se corrigió `app/globals.css` quitando el `display:flex` centrador de `body` (remanente del mockup viejo, incompatible con el `<nav>+<main>` de layout.tsx). Se reescribió `app/layout.tsx` con una tarjeta tipo teléfono (responsive: decorativa y centrada en desktop vía breakpoint `sm:`, a pantalla completa sin bordes en un celular real) y se creó `app/components/BottomNav.tsx` (client component, 5 tabs: Inicio/Mascotas/Protectoras/Guías/Perfil, oculto en `/login` y `/registro`, resalta la ruta activa con `usePathname`). Se cambiaron todos los `min-h-screen` de las páginas a `min-h-full` (ya no son pantallas de navegador completas, sino contenido dentro de la tarjeta). Se sacaron los breakpoints `sm:`/`lg:` de las grillas y flex de landing, `/mascotas`, `/protectoras` y el selector de `/registro`, porque esos breakpoints responden al ancho de la ventana del navegador (no al ancho de la tarjeta angosta), y en desktop hacían aparecer columnas de más aunque la tarjeta fuera angosta.
- Archivos tocados: `app/globals.css`, `app/layout.tsx` (reescrito), `app/components/BottomNav.tsx` (nuevo), `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/perfil/page.tsx`. Backup nuevo: `documents/mds/Fupa nuevo/backup-page-vacunacion-agenda.tsx.txt`.
- Estado: completo. `npm run build` sin errores. Verificado en navegador real (Chrome del sistema vía Playwright) en dos anchos: desktop (1440px, muestra la tarjeta decorativa centrada con esquinas redondeadas) y celular real (390×844, ocupa toda la pantalla sin bordes ni esquinas redondeadas). Sin errores de consola. Confirmado que el nav inferior resalta la pestaña activa y se oculta en `/login`/`/registro`.
- Nota: el usuario todavía no dio instrucciones sobre qué hacer con la funcionalidad huérfana de vacunación/agenda/perfil (ver entrada 2) — dijo explícitamente "luego te digo qué hacer con lo otro". No tocar ese tema hasta que lo pida.
- Próximo paso sugerido: esperar instrucción del usuario sobre vacunación/agenda/perfil.

## 6 — Prompt 06 (detalle real + link registro) y prompt 07 (libreta de vacunación mock)
- Qué se hizo: (Prompt 06) `app/mascotas/[id]/page.tsx` ahora busca la mascota en `mockMascotas` por id y muestra ficha completa (imagen/emoji, nombre, raza, edad, tamaño, descripción) o "Mascota no encontrada" si no existe. `app/protectoras/[id]/page.tsx` ídem con `mockProtectoras`. `app/(auth)/registro/page.tsx` tiene ahora un link "¿Ya tenés cuenta? Iniciá sesión" hacia `/login`, con el mismo estilo que el link recíproco de login. (Prompt 07) `data/mock-mascotas.ts` extendió `MascotaMock` con el campo opcional `vacunas` (nombre, fecha DD/MM/AAAA, estado APLICADA/PENDIENTE) y se cargaron datos de ejemplo en Firulais (3 vacunas caninas) y Michi (2 vacunas felinas); Rocky y Luna quedaron sin el campo a propósito para probar el caso "sin vacunas". `app/mascotas/[id]/page.tsx` agregó la sección "Libreta de vacunación" después de la ficha, renderizada solo si `mascota.vacunas` existe, con badge verde (Aplicada) o dorado (Pendiente) por vacuna. Es 100% mock, sin calendario, sin protocolos WSAVA, sin Capacitor, tal como pedía el prompt.
- Bug encontrado y corregido en el camino: `params` en las páginas dinámicas (`/mascotas/[id]`, `/protectoras/[id]`) es una `Promise` en Next.js 16, no un objeto síncrono. El código original (heredado de 02a) lo trataba como objeto plano, así que `params.id` daba `undefined` y **toda mascota/protectora mostraba "no encontrada"** aunque el id fuera válido. Se detectó al verificar visualmente (no con el build, que no lo marca como error) y se corrigió haciendo los componentes `async` y agregando `await params`.
- Archivos tocados: `app/mascotas/[id]/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/(auth)/registro/page.tsx`, `data/mock-mascotas.ts`.
- Estado: completo. `npm run build` sin errores. Verificado visualmente en navegador real: `/mascotas/1` (con libreta de vacunación, 2 aplicadas + 1 pendiente), `/mascotas/3` (sin vacunas, sección ausente), `/mascotas/999` (mensaje "Mascota no encontrada"), `/protectoras/1` (ficha de FUPA), `/registro` (link a login visible). Sin errores de consola.
- Próximo paso sugerido: no quedan más prompts numerados pendientes de implementar (el 05/backend está pausado explícitamente por el usuario hasta después de la presentación). Esperar instrucciones.

## 7 — Prompt 08 (sesión mock con rol + perfil condicional)
- Qué se hizo: Se creó `app/context/AuthContext.tsx` (Context + localStorage, tal cual el snippet del prompt) que expone `loggedIn`, `role`, `nombre`, `login()` y `logout()`. Se envolvió el contenido de `app/layout.tsx` con `<AuthProvider>`. En `app/(auth)/login/page.tsx` se agregó un selector de rol (Adoptante/Protectora) con validación simple (si no se elige rol, no envía y muestra un aviso en rojo); al enviar, llama a `login(email.split("@")[0], rolSeleccionado)` y redirige a `/perfil` con `useRouter`. En `app/(auth)/registro/page.tsx`, ambos formularios (adoptante y protectora) ahora llaman a `login(nombre|nombreProtectora, tipoUsuario)` y redirigen a `/perfil`, además del alert mock que ya tenían. `app/perfil/page.tsx` se reescribió completo: sin sesión muestra invitación a loguearse; con sesión muestra "Hola, {nombre}" + contenido condicional según `role` (solicitudes/explorar mascotas para adoptante; mascotas publicadas/solicitudes recibidas/datos de la protectora para protectora) + botón de cerrar sesión. Se usó el alias `@/app/context/AuthContext` en vez de rutas relativas para evitar errores de profundidad de carpetas.
- Ajuste propio (no especificado en el prompt): se usó `min-h-full` en vez de `min-h-screen` en `/perfil` y `text-red-600` (Tailwind estándar) en vez de una variable CSS custom para el aviso de error, siguiendo la convención ya establecida en toda la app desde que se armó el shell de "app de celular" (entrada 5) — `min-h-screen` rompería el layout dentro de la tarjeta, y no existe una variable `--red-alert` en `:root`.
- Archivos tocados: `app/context/AuthContext.tsx` (nuevo), `app/layout.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/perfil/page.tsx`.
- Estado: completo. `npm run build` sin errores. Probado en navegador real con Playwright, flujo completo: intento de login sin elegir rol (bloquea y avisa) → login como adoptante → `/perfil` muestra "Cuenta de adoptante" con sus opciones → cerrar sesión → `/perfil` vuelve a pedir login → login como protectora → `/perfil` muestra "Cuenta de protectora" con sus opciones. Sin errores de consola en ningún paso.
- Próximo paso sugerido: no quedan prompts numerados pendientes (05/backend sigue pausado). Esperar instrucciones del usuario.

## 8 — Prompt 09 (adoptar requiere login, con redirect de vuelta)
- Qué se hizo: `app/mascotas/[id]/page.tsx` pasó de Server Component async a Client Component (necesitaba `useAuth`/`useRouter`). Como `params` sigue siendo una `Promise` en Next 16 y un client component no puede usar `await` en el cuerpo, se reemplazó `await params` por el hook `use(params)` de React (disponible porque el proyecto usa React 19). Se agregó el botón "Quiero adoptar a {nombre}" dentro de la ficha, con `handleAdoptar()`: sin sesión redirige a `/login?redirect=/mascotas/{id}`; con sesión y rol protectora muestra alert de que las protectoras no pueden adoptar; con sesión y rol adoptante muestra el alert mock de solicitud enviada. En `app/(auth)/login/page.tsx` se agregó `useSearchParams` para leer el parámetro `redirect` y usarlo en el `router.push` tras loguearse (en vez de siempre ir a `/perfil`), dejando intacto el flujo de registro como pedía el prompt.
- Problema encontrado y resuelto: el build falló porque Next.js exige que `useSearchParams` esté envuelto en un `<Suspense>` (si no, no puede prerenderizar la página). Se resolvió separando el formulario en un componente interno `LoginForm` y envolviéndolo con `<Suspense fallback={null}>` en el `LoginPage` exportado — no estaba en el prompt, era necesario para que compilara.
- Archivos tocados: `app/mascotas/[id]/page.tsx`, `app/(auth)/login/page.tsx`.
- Estado: completo. `npm run build` sin errores. Probado en navegador real con Playwright el flujo de punta a punta: sin sesión, click en "Quiero adoptar a Firulais" en `/mascotas/1` → redirige a `/login?redirect=/mascotas/1` (confirmado por URL y captura) → login como adoptante → vuelve exactamente a `/mascotas/1` (no a `/perfil`, confirmado por URL y captura) → click en adoptar de nuevo con sesión de adoptante → alert "Solicitud de adopción enviada (mock)" → logout, login como protectora, click en adoptar → alert "Las cuentas de protectora no pueden enviar solicitudes de adopción". Los 4 casos se comportaron como se especificaba.
- Próximo paso sugerido: no quedan prompts numerados pendientes. Esperar instrucciones del usuario.

## 9 — Ajuste: ocultar botón de adoptar para cuentas de protectora
- Qué se hizo: el usuario pidió que, ya que las protectoras no pueden adoptar (mensaje de bloqueo agregado en el prompt 09), directamente no vean el botón. En `app/mascotas/[id]/page.tsx` se envolvió el botón "Quiero adoptar a {nombre}" en `{role !== "protectora" && (...)}` y se sacó del `handleAdoptar` el chequeo de rol protectora (ya no hace falta, si el botón no está no se puede ni ejecutar). El botón sigue visible sin sesión (para no romper el flujo de redirect a `/login`) y logueado como adoptante.
- Archivos tocados: `app/mascotas/[id]/page.tsx`.
- Estado: completo. `npm run build` sin errores. Verificado en navegador real: sin sesión el botón aparece (dispara redirect a login); logueado como protectora el botón desaparece (conteo de elementos = 0, sin alert posible); logueado como adoptante el botón vuelve a aparecer.
- Próximo paso sugerido: esperar instrucciones del usuario.

## 10 — Prompt 10 (perfil de protectora funcional, mock end-to-end)
- Qué se hizo: Se crearon `app/context/MascotasContext.tsx` (reemplaza el import estático de `mockMascotas` por un Context con `localStorage`, expone `mascotas` y `addMascota`) y `app/context/SolicitudesContext.tsx` (maneja `solicitudes` en `localStorage`, expone `addSolicitud` y `actualizarEstado`), ambos tal cual el prompt (con el import de `mock-mascotas` vía alias `@/data/...` en vez de ruta relativa). `app/layout.tsx` se envolvió con `<MascotasProvider>` y `<SolicitudesProvider>` dentro de `<AuthProvider>`. `app/mascotas/page.tsx` y `app/mascotas/[id]/page.tsx` pasaron a usar `useMascotas()` en vez del array estático. En `[id]/page.tsx`, `handleAdoptar` ahora llama a `addSolicitud({ mascotaId, mascotaNombre, adoptanteNombre: nombre })` (usando el `nombre` de `useAuth()`) y muestra el alert de confirmación, en vez del alert genérico anterior. En `app/perfil/page.tsx`: la sección de adoptante muestra sus propias solicitudes reales (filtradas por `adoptanteNombre === nombre`) con badge verde/dorado/borde-rojo según estado; la sección de protectora muestra la cantidad real de mascotas + un formulario colapsable para agregar una mascota nueva (nombre, especie, raza, edad aproximada, tamaño, descripción) que llama a `addMascota`, y la lista real de solicitudes recibidas con botones "Aprobar"/"Rechazar" que llaman a `actualizarEstado` (solo visibles si la solicitud está PENDIENTE).
- Archivos tocados: `app/context/MascotasContext.tsx` (nuevo), `app/context/SolicitudesContext.tsx` (nuevo), `app/layout.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/perfil/page.tsx`.
- Estado: completo. `npm run build` sin errores. Probado el flujo completo de punta a punta en navegador real (Playwright): login como protectora "fupa" → agregar mascota "Toby" (perro, Beagle, 1 año, pequeño) vía formulario → contador pasa de 4 a 5 mascotas publicadas → logout → login como adoptante "ana" → `/mascotas` muestra a Toby → entrar al detalle de Toby → "Quiero adoptar a Toby" → alert de confirmación → logout → login como protectora de nuevo → `/perfil` muestra la solicitud de "ana" por "Toby" con fecha y estado Pendiente + botones Aprobar/Rechazar → click en Aprobar → badge cambia a "Aprobada" y los botones desaparecen. Sin errores de consola en ningún paso. Todo persiste en `localStorage` del navegador (sin backend), tal como preveía el prompt.
- Próximo paso sugerido: esperar instrucciones del usuario. Recordar que este estado (mascotas agregadas, solicitudes) vive solo en el localStorage del navegador de la demo — no sobrevive a otra computadora ni a borrar datos de navegación.

## 11 — Ajuste: ocultar botones "Quiero adoptar" / "Soy una protectora" del hero si ya hay sesión
- Qué se hizo: el usuario pidió que, si ya estás logueado, no tiene sentido ver los botones de "Quiero adoptar" / "Soy una protectora" en la landing (`/`) — deberían reaparecer recién al cerrar sesión. `app/page.tsx` pasó a Client Component (`"use client"`), se importó `useAuth`, y se envolvió ese bloque de dos botones en `{!loggedIn && (...)}`. El resto del hero (título y descripción) se mantiene siempre visible.
- Archivos tocados: `app/page.tsx`.
- Estado: completo. `npm run build` sin errores. Verificado en navegador real: sin sesión aparece 1 botón "Quiero adoptar" (conteo de elementos); logueado como adoptante, 0; tras cerrar sesión, vuelve a 1. Sin errores de consola.
- Próximo paso sugerido: esperar instrucciones del usuario.

## 12 — Prompt 11 (foto/video en el formulario de agregar mascota, mock)
- Qué se hizo: `data/mock-mascotas.ts` sumó los campos opcionales `mediaUrl` y `mediaType` ("image" | "video") a `MascotaMock`. `app/context/MascotasContext.tsx` actualizó `addMascota` para que el estado en memoria (`setMascotas`) incluya el media completo, pero antes de guardar en `localStorage` se despoja `mediaUrl`/`mediaType` de cada mascota (map con destructuring), tal cual pedía el prompt — así no se rompe la cuota de almacenamiento con blobs de imagen/video. En `app/perfil/page.tsx`, el formulario de agregar mascota (sección protectora) sumó un input de archivo (`accept="image/*,video/*"`) que genera una URL local con `URL.createObjectURL()`, dos `useState` nuevos (`mediaUrl`, `mediaType`) y una vista previa (`<img>` o `<video controls>` según corresponda) debajo del input; al publicar, esos valores se mandan a `addMascota` y se resetean junto con el resto del formulario. `app/mascotas/page.tsx` y `app/mascotas/[id]/page.tsx` ahora muestran esa imagen/video en vez del emoji placeholder cuando la mascota tiene `mediaUrl` (h-32 en el listado, h-64 en el detalle).
- Bug encontrado y corregido en el camino: el link "Ver más" de `app/mascotas/page.tsx` usaba `<a href>` en vez de `next/link`, lo que forzaba una recarga completa de página al entrar al detalle — y como el media vive solo en memoria (no en localStorage, por diseño de este mismo prompt), esa recarga lo hacía desaparecer inmediatamente, incluso dentro de la misma sesión. Se cambió a `<Link>` de `next/link` para que la navegación sea client-side (SPA) y no destruya el estado de `MascotasContext`. Sin este cambio, la funcionalidad pedida en este prompt no era demostrable como se especifica ("sin recargar la página").
- Archivos tocados: `data/mock-mascotas.ts`, `app/context/MascotasContext.tsx`, `app/perfil/page.tsx`, `app/mascotas/page.tsx` (incluye el fix del link), `app/mascotas/[id]/page.tsx`.
- Estado: completo. `npm run build` sin errores. Probado en navegador real subiendo un archivo de imagen de verdad (`public/assets/logo.jpg`) desde el formulario: la vista previa aparece en el formulario; al publicar, la mascota nueva ("Coco") se ve con esa foto en `/mascotas` (navegando por el tab "Mascotas" del nav inferior, sin recargar) y en su página de detalle (navegando por "Ver más", confirmado por el cambio de URL a `/mascotas/{id}` y el conteo de `<img>` en esa página específica). Sin errores de consola.
- Aclaración para la demo (igual que en el prompt): el media NO persiste a una recarga de página — es una `blob:` URL que vive únicamente en memoria de esa pestaña. Si se recarga `/mascotas` o `/perfil`, la mascota sigue existiendo (eso sí persiste en localStorage) pero vuelve a mostrar el emoji placeholder.
- Próximo paso sugerido: esperar instrucciones del usuario. Si se quiere que alguna mascota de ejemplo tenga foto desde el arranque (sin depender de subirla en vivo), hace falta un prompt aparte para precargar una URL externa fija en los datos mock (según la nota del propio documento 11).

## 13 — Commit de checkpoint + instrucciones.md para un compañero
- Qué se hizo: Se creó `instrucciones.md` en la raíz con la guía para que un compañero clone/pullee el repo y pueda correr el proyecto (requisitos de Node/npm, `npm install`, sin variables de entorno necesarias por ahora, `npm run dev`, qué leer antes de tocar código —`AGENTS.md` y `PROGRESS_LOG.md`—, cómo está armado hoy —Contexts mock en localStorage, cada uno ve sus propios datos de prueba—, y convenciones de código a respetar). Antes de esto se hizo un commit de checkpoint (`git add . && git commit`) a pedido del usuario; se detectó que `.opencode/` (caché de otra herramienta de IA, ~61MB con su propio `node_modules`) estaba sin trackear y a punto de colarse en el commit, así que se agregó a `.gitignore` antes de commitear.
- Archivos tocados: `.gitignore` (agregado `.opencode/`), commit `bdbf9d6` con 37 archivos (todo el trabajo de las entradas 1 a 12 de este log), `instrucciones.md` (nuevo, todavía sin commitear).
- Estado: completo. El commit de checkpoint se hizo localmente, no se pusheó a GitHub (no se pidió explícitamente). `instrucciones.md` queda pendiente de agregar a un próximo commit si el usuario lo pide.
- Próximo paso sugerido: preguntar si hay que pushear el commit y/o commitear `instrucciones.md`.

## 14 — Prompt 5 (setup MySQL + Prisma): bloqueado en el paso 2, falta schema.prisma
- Qué se hizo: AGENTS.md fue actualizado (por el usuario/Claude de escritorio) confirmando que la fase visual/mock terminó y arranca la fase de backend real, así que se procedió con el prompt 5. Paso 1: se instalaron `prisma` (devDependency, `^8.0.0-rc.13`) y `@prisma/client` (dependency, `^7.10.0`) — confirmado en `package.json`. Paso 3: se confirmó que existe `.env` en la raíz y que tiene una línea `DATABASE_URL=`, sin leer ni mostrar su contenido; también se confirmó que `.env*` ya está en `.gitignore` (no hace falta agregarlo).
- Bloqueo encontrado: el paso 2 pide copiar `documents/mds/Fupa nuevo/schema.prisma` hacia `prisma/schema.prisma`, pero **ese archivo no existe** — se buscó en toda la carpeta y en todo el repo (`find -iname "*.prisma"`) y no hay ningún `.prisma` en ningún lado. Solo está `05-setup-mysql-prisma.md` (las instrucciones), no el schema en sí. No se fabricó un schema propio para no inventar un modelo de datos que ya estaba "diseñado" en otro lado (según la nota del prompt 07, que referencia un `Vacunacion` del `schema.prisma` "que ya está diseñado"). Por lo tanto, los pasos 4 (`prisma migrate dev`) y 5 (`prisma generate`) NO se ejecutaron, porque no hay ningún schema que migrar/generar todavía.
- Archivos tocados: `package.json`, `package-lock.json` (por el `npm install`). No se creó `prisma/`, no se tocó `.env`, no se tocó ningún otro archivo — tal como pedía el prompt.
- Observación menor (no bloqueante, no corregida): `prisma` quedó en `^8.0.0-rc.13` (release candidate) y `@prisma/client` en `^7.10.0` — son majors distintos. Prisma normalmente espera que CLI y client tengan la misma versión. Como igual estamos bloqueados en el paso 2, no se tocó esto; conviene revisarlo cuando se resuelva el schema faltante.
- Estado: parcial / bloqueado. Reportado al usuario para que confirme dónde está el schema real o lo suba a `documents/mds/Fupa nuevo/schema.prisma`.
- Próximo paso sugerido: esperar que el usuario provea el contenido de `schema.prisma`, después retomar desde el paso 2.

## 15 — Prisma pinneado a 7.10.0, migrate/generate fallan por incompatibilidad de schema
- Qué se hizo: se corrigió el desfasaje de versiones — `npm install prisma@latest @prisma/client@latest --save-exact` seguía trayendo `prisma` en release candidate (`8.0.0-rc.13`) mientras `@prisma/client@latest` daba `7.10.0` estable, así que se aplicó el fallback pedido: `npm install prisma@7.10.0 @prisma/client@7.10.0 --save-exact`. Confirmado en `package.json`: ambos paquetes quedaron en `7.10.0` exacto. `documents/mds/Fupa nuevo/schema.prisma` ya existía (subido por el usuario), se copió tal cual a `prisma/schema.prisma` (carpeta creada).
- Bloqueo encontrado: tanto `npx prisma migrate dev --name init` como `npx prisma generate` fallan con el mismo error, **antes de siquiera intentar conectarse a la base de datos** — no es un error de credenciales ni de MySQL caído. Es el código `P1012`: en Prisma 7, la propiedad `url` dentro del bloque `datasource` del `schema.prisma` (`url = env("DATABASE_URL")`) ya no está soportada — Prisma 7 cambió el modelo de configuración: ahora la URL de conexión va en un archivo `prisma.config.ts` nuevo, y hay que pasarle un `adapter` (ej. `@prisma/adapter-mysql` o similar) al constructor de `PrismaClient` en el código, en vez de que el datasource la lea directo del schema. El `schema.prisma` que se copió está escrito con la convención vieja (probablemente pensado para Prisma 5/6), incompatible tal cual con Prisma 7.
- Archivos tocados: `package.json`, `package-lock.json` (versiones de Prisma), `prisma/schema.prisma` (nuevo, copiado tal cual del original).
- Estado: bloqueado. No se ejecutó ninguna migración real, no se generó el client, no se tocó `.env` ni se intentó adivinar nada. Reportado al usuario con las dos opciones posibles (bajar la versión de Prisma para que coincida con cómo está escrito el schema, o migrar el schema/config al patrón nuevo de Prisma 7) para que decida con Tinu.
- Próximo paso sugerido: esperar decisión del usuario sobre qué versión de Prisma usar (ligada a cómo está escrito el schema.prisma que ya tienen diseñado).

## 16 — Prisma bajado a 6.x, migración exitosa contra MySQL real
- Qué se hizo: se corrió `npm install prisma@6 @prisma/client@6 --save-exact`. Confirmado en `package.json`: ambos paquetes quedaron en `6.19.3` exacto (misma major, coinciden). Con esa versión, `npx prisma migrate dev --name init` corrió sin errores: se conectó a `MySQL "happy_paws" en localhost:3306`, creó la migración `20260909015712_init` y la aplicó. Después `npx prisma generate` regeneró el client igual (v6.19.3) sin errores — Prisma avisó que hay una versión mayor disponible (8.0.0-rc.13) pero no se actualizó, tal como se decidió.
- Verificación: se leyó el `migration.sql` generado y las 8 tablas creadas coinciden exactamente con las que esperaba la sección "Verificación" de `documents/mds/Fupa nuevo/05-setup-mysql-prisma.md`: `users`, `protectoras`, `miembros_protectora`, `mascotas`, `razas`, `vacunaciones`, `adoptantes`, `solicitudes_adopcion`.
- Archivos tocados: `package.json`, `package-lock.json` (Prisma a 6.19.3), `prisma/migrations/20260909015712_init/migration.sql` (nuevo, generado por Prisma), `node_modules/@prisma/client` (regenerado). Se actualizó el checklist en `AGENTS.md`: `[x] MySQL + Prisma conectado y migrado`.
- Estado: completo, sin errores. Base de datos real `happy_paws` ahora tiene las 8 tablas del schema. El Prisma Client tipado está generado y disponible para usar en el código (`@prisma/client`).
- Próximo paso sugerido: empezar a reemplazar los Contexts mock (`AuthContext`, `MascotasContext`, `SolicitudesContext`) por llamadas reales usando el Prisma Client, según indica la fase actual de AGENTS.md — probablemente necesite un prompt aparte por pieza (auth real primero, dado que todo lo demás depende de usuarios reales).

## 17 — Prompt 12 (rediseño completo: de app-shell mobile a página web estándar)
- Qué se hizo: se ejecutaron las 7 tareas del prompt. (1) `app/layout.tsx` perdió el div que simulaba la tarjeta de teléfono (ancho fijo, bordes redondeados, `h-dvh`); ahora `<body>` contiene directamente `<Navbar />`, `{children}` y un `<footer>` simple. (2) Se creó `app/components/Navbar.tsx` (client component): logo "Happy Paws" a la izquierda, links horizontales Inicio/Mascotas/Protectoras/Guías/Perfil desde `md:` en adelante con resaltado de ruta activa (mismo criterio que tenía `BottomNav`, vía `usePathname`), botón hamburguesa con `useState` en mobile que despliega los mismos links en vertical, fondo `brown-dark`/texto claro, visible en TODAS las rutas incluyendo `/login` y `/registro`. Se borró `app/components/BottomNav.tsx` y toda referencia a él (confirmado con búsqueda, no quedó ninguna). (3) `app/page.tsx`: se reintrodujeron los breakpoints reales (`sm:grid-cols-2 lg:grid-cols-4` en mascotas destacadas, `sm:grid-cols-2` en protectoras, `sm:grid-cols-3` en "cómo funciona" — este último no estaba presente pese a que AGENTS.md decía que ya existía, así que se agregó) y se envolvió el contenido de cada sección con grid en `mx-auto max-w-6xl`. (4) `/mascotas` y `/protectoras`: mismos breakpoints (`sm:grid-cols-2 lg:grid-cols-4` y `sm:grid-cols-2` respectivamente) + `mx-auto max-w-6xl`. (5) `/mascotas/[id]`: layout de dos columnas en desktop (`grid grid-cols-1 lg:grid-cols-2 gap-8`, media a la izquierda, ficha+botón de adoptar a la derecha) dentro de `mx-auto max-w-4xl`, una sola columna en mobile. `/protectoras/[id]`: una columna más ancha con `mx-auto max-w-4xl`, sin grid de dos columnas (tal como pedía el prompt). (6) `/login` y `/registro`: confirmado que ya usaban `max-w-sm`/`w-full max-w-sm` (no hacía falta tocar eso), no había ningún contenedor extra heredado del app-shell viejo. (7) `/perfil`: `mx-auto max-w-3xl` agregado en ambos estados (logueado y no logueado), como wrapper interno para no perder el fondo a todo el ancho.
- Ajuste no pedido explícitamente pero necesario: todos los `min-h-full` de estas páginas (dependían de la cadena de alturas `h-dvh` del app-shell que ya no existe) se cambiaron a `min-h-screen`, si no las páginas no llegaban a cubrir el alto de la pantalla con su color de fondo.
- Archivos tocados: `app/layout.tsx`, `app/components/Navbar.tsx` (nuevo), `app/components/BottomNav.tsx` (borrado), `app/page.tsx`, `app/mascotas/page.tsx`, `app/protectoras/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/perfil/page.tsx`.
- Verificación: `npm run build` compiló sin errores (10 rutas generadas). **No se pudo verificar visualmente en un navegador real esta vez** — a diferencia de sesiones anteriores, en este entorno no se encontró Chrome/Edge instalado ni caché de Playwright, así que no se pudo levantar Playwright como en los prompts 04/08/09/10/11. En su lugar se verificó: (a) el HTML estático generado (`.next/server/app/index.html`) contiene las clases `sm:grid-cols-2`, `lg:grid-cols-4`, el texto "Happy Paws" del navbar y las clases `hidden items-center gap-6 md:flex` / `md:hidden` que separan el nav horizontal del botón hamburguesa; (b) el CSS compilado (`.next/static/chunks/*.css`) efectivamente define esas clases (`grid-template-columns: repeat(4,...)`, `repeat(3,...)`, `repeat(2,...)`, `position: sticky`, `max-w-6xl`/`max-w-4xl`/`max-w-3xl`, colores `bg-brown-dark`/`text-brown-lightest`/`border-brown-light`) — es decir, no solo que el JSX pide esas clases sino que Tailwind las generó. Esto confirma que el mecanismo responsive está correctamente cableado, pero **falta la verificación visual real en un navegador** (mobile vs desktop, hamburguesa abriendo/cerrando) que si hay Chrome disponible en otra sesión convendría hacer antes de dar esto por 100% cerrado.
- Rama: `feature/rediseno-web` (creada desde `main` actualizado, siguiendo `WORKFLOW-GIT.md`). Falta pushear y abrir el PR.
- Estado: completo a nivel de código y build; verificación visual en navegador real pendiente por falta de browser en este entorno.
- Próximo paso sugerido: pushear `feature/rediseno-web`, abrir el PR (`gh` tampoco está disponible acá, así que manual desde GitHub), y pedirle a alguien con navegador a mano que confirme visualmente el navbar/grids en mobile y desktop antes de mergear. Después: retomar auth real (`feature/auth-real`) como venía indicado en la entrada 16.
- Actualización: el usuario probó `/` en su propio navegador (Brave) y confirmó que el Navbar se ve bien, pero reportó 404 en el body de la página. Causa: tenía un `npm run dev` corriendo contra una carpeta `.next` que mezclaba artefactos de mi `npm run build` (producción) con los del dev server, dejando el routing manifest inconsistente. Se paró ese proceso, se borró `.next` y se volvió a levantar `npm run dev` limpio — `/`, `/mascotas` y `/perfil` respondieron 200. Lección para la próxima: no correr `next build` y `next dev` sobre la misma carpeta `.next` sin limpiar entre uno y otro.

## 18 — Refresh visual: tipografía, botones/cards reutilizables, gradientes y detalles cálidos (pedido directo del usuario, sin prompt numerado)
- Qué se hizo: el usuario pidió, hablando directamente conmigo (no vía un prompt de `documents/mds/Fupa nuevo/`), darle a la web "un toque más vivo, actual y moderno" sin perder la calidez ni tocar la paleta de colores de AGENTS.md. Se hizo un pase visual sobre toda la app ya migrada a página web (entrada 17), manteniendo los valores exactos de `--brown-*`/`--gold`/etc.: (1) Tipografía: se agregaron las Google Fonts `Fredoka` (títulos, vía `next/font/google`, variable `--font-heading-family`) y `Nunito` (texto, variable `--font-body-family`) en `app/layout.tsx`; en `globals.css` se conectaron a los tokens `--font-sans`/`--font-heading` de Tailwind, y una regla `h1,h2,h3 { font-family: var(--font-heading) }` aplica la tipografía de título automáticamente sin tener que tocar cada encabezado. (2) Se agregaron clases reutilizables en `@layer components` de `globals.css`: `.btn-primary` (dorado, para CTAs principales), `.btn-secondary` (outline claro, sobre fondos oscuros), `.btn-dark`, `.btn-outline`, `.card` (borde + hover con elevación y sombra), `.badge-pill` (etiqueta tipo especie/rol) y `.input` (con focus ring) — reemplazan los strings de clases largos y duplicados que había sueltos en cada página. (3) `Navbar.tsx`: fondo con transparencia + `backdrop-blur` (efecto vidrio esmerilado), logo con círculo dorado y 🐾, indicador de ruta activa tipo píldora, menú mobile con transición de altura suave (`grid-rows-[0fr]→[1fr]`) en vez de aparecer/desaparecer abruptamente. (4) Landing (`page.tsx`): hero con degradé `brown-dark→brown-darker`, dos manchas decorativas difuminadas (`blur-3xl`) en las esquinas, badge "🐾 Adopción responsable en Villa Carlos Paz", título con la palabra "mejor amigo" resaltada en dorado, animación de entrada sutil (`animate-fade-in-up`), tarjetas de mascotas con badge de especie e ícono que escala al hover, sección "cómo funciona" con íconos de `lucide-react` (Search/ClipboardCheck/HeartHandshake) en círculos con degradé en vez de números pelados. (5) `/mascotas`, `/protectoras`, `/mascotas/[id]`, `/protectoras/[id]`: mismas cards con hover-lift, badges de especie, sombras suaves. (6) `/login`, `/registro`, `/perfil`: fondo con degradé sutil detrás del formulario, avatar circular con inicial del nombre en `/perfil`, botones e inputs unificados con las clases nuevas. (7) `/guias`: de yapa (no estaba en el alcance del prompt 12 ni de este pedido, pero tenía el mismo `min-h-full` viejo y ningún ancho máximo) se alineó al mismo criterio: `min-h-screen`, `mx-auto max-w-3xl`, tarjetas con `.card`. Se aprovechó también para borrar las `@keyframes` huérfanas (`foodFly`, `petFloat`, `petChew`, `petJump`, `shadowPulse`) que quedaban de la vieja mascota virtual en `globals.css` — no las usaba nada, confirmado con búsqueda.
- Archivos tocados: `app/globals.css`, `app/layout.tsx`, `app/components/Navbar.tsx`, `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`.
- Verificación: `npm run build` compiló sin errores (mismas 10 rutas). Se probó de nuevo instalar Chromium para Playwright (el intento anterior parecía trabado y se había cortado) — terminó de descargar en segundo plano pero `chrome.exe` no apareció en la carpeta extraída (solo el `.dll`), probablemente por cuarentena de antivirus sobre un ejecutable sin firmar bajado por script; se descartó ese camino. En su lugar se levantó `npm run dev` limpio y se verificó con `curl` que las 7 rutas principales responden 200, y se inspeccionó el HTML servido de `/` confirmando que las clases nuevas (`font-heading`, `badge-pill`, `btn-primary`, `animate-fade-in-up`, el degradé del hero) están presentes, y que `<html>` carga las clases de variable de Fredoka y Nunito generadas por `next/font` (confirma que las fuentes se están aplicando, no solo que el código las referencia). **Sigue pendiente la revisión visual real en navegador** — se lo dejo al usuario, que ya tiene la app abierta.
- Estado: completo a nivel de código, build y verificación de HTML/CSS servido; falta el visto bueno visual del usuario en su propio navegador.
- Próximo paso sugerido: que el usuario recargue `localhost:3000` y confirme si el resultado visual lo convence (tipografía, hero, cards) o si quiere ajustar algo puntual. Después: commitear esta entrada + los cambios en `feature/rediseno-web` (ya pusheada, así que esto sería un commit adicional al mismo PR todavía sin mergear) y retomar auth real.

## 19 — Logo real en vez del emoji 🐾, y endurecimiento de responsive
- Qué se hizo: el usuario pidió dos cosas puntuales. (1) Reemplazar el círculo dorado con el emoji 🐾 que se usó como "logo" junto al título "Happy Paws" en el Navbar por el logo real del proyecto, que ya existía en `public/assets/logo.jpg` (pata con corazón, perro y gato, 460×460, fondo blanco). Se usó `next/image` apuntando a `/assets/logo.jpg` dentro de un círculo `rounded-full bg-white object-cover`, para que el fondo blanco del jpg se recorte prolijo contra el fondo oscuro del navbar en vez de mostrarse como un cuadrado. Se aprovechó la misma consistencia de marca para reemplazar los otros círculos con 🐾 que se habían agregado como decoración en la entrada 18 (encabezado de `/login`, `/registro`, `/perfil` sin sesión, y el ícono del footer en `layout.tsx`) por el mismo logo real — no se tocaron los emoji que son parte del copy (badge del hero, botón "Quiero adoptar 🐾", los íconos 🐾/🏠 de los botones selectores de tipo de cuenta en `/registro`), esos no son "el logo", son decoración de texto/opciones. (2) Auditoría de responsive en toda la app: se revisó cada archivo de `app/` buscando anchos fijos en píxeles, `100vw`/`h-dvh` remanentes, grids sin fallback a una columna en mobile y filas flex con texto que podría desbordar con datos reales más largos que los mock. No se encontraron anchos fijos ni unidades de viewport problemáticas (ya se habían eliminado con el app-shell en la entrada 17). Se reforzaron tres puntos donde un nombre de mascota/adoptante largo (con datos reales, no los mock cortos actuales) podía apretar el badge de estado al lado: la lista de "Mis solicitudes" y "Solicitudes recibidas" en `/perfil`, y la lista de vacunas en `/mascotas/[id]` — se les agregó `min-w-0` + `truncate` al texto y `shrink-0` al badge, para que el texto largo corte con "…" en vez de empujar o desbordar en pantallas angostas.
- Archivos tocados: `app/components/Navbar.tsx`, `app/layout.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/perfil/page.tsx`, `app/mascotas/[id]/page.tsx`.
- Verificación: `npm run build` compiló sin errores (mismas 10 rutas). Se restablecieron los mismos problemas que en la entrada 17 al correr `build` con el `npm run dev` del usuario corriendo en simultáneo sobre la misma carpeta `.next` — otra vez quedó en 500 hasta que se paró el proceso, se borró `.next` y se levantó un `npm run dev` limpio (confirmado con `curl`: `/`, `/mascotas`, `/protectoras`, `/perfil`, `/login`, `/registro`, `/guias`, `/mascotas/1` devuelven 200). Se confirmó además con `curl` que `/assets/logo.jpg` responde 200 con `content-type: image/jpeg` y que el HTML de `/` referencia esa ruta. Sigue sin haber navegador real disponible en este entorno (Chromium de Playwright terminó de descargar pero sin `chrome.exe`, presumiblemente por cuarentena de antivirus) — la verificación visual final (que el logo se vea bien recortado en el círculo, que no haya scroll horizontal en mobile) queda para el usuario en su propio navegador.
- Estado: completo a nivel de código y build; verificación visual final pendiente del usuario.
- Próximo paso sugerido: confirmar con el usuario que el logo se ve bien y que no encontró scroll horizontal ni elementos cortados en su celular/ventana angosta. Commitear y pushear esta entrada al mismo PR de `feature/rediseno-web`, y recordar (otra vez) no correr `npm run build` mientras el `npm run dev` del usuario está activo sobre la misma carpeta.

## 20 — Merge de feature/rediseno-web a main (diagnóstico: "se ve como celular" no era caché)
- Qué se hizo: el usuario reportó que la web se veía como un mockup de celular en vez de una página web completa y pidió revisar si era un caché viejo. Se hizo `git pull` primero (a pedido explícito del usuario en el mensaje anterior), lo que trajo 3 ramas nuevas del remoto sin mergear: `chore/guardar-config-y-docs`, `chore/workflow-equipo`, `feature/rediseno-web`. Se diagnosticó el reporte del celular leyendo `app/layout.tsx`/`globals.css` de `main`: no era un resto de caché, era el shell de "app de celular" (tarjeta centrada `sm:max-w-[420px]`, `BottomNav`) que se armó a propósito en la entrada 5 — pero `main` todavía no tenía el trabajo de las entradas 17-19 de este mismo log (rediseño a página web completo, ya hecho pero solo en la rama `feature/rediseno-web`, subida por un compañero/otra sesión y todavía sin mergear). Confirmado con `git log`/`git diff --stat` que esa rama sale del mismo commit que `main` (`684ad7c`) y contiene exactamente ese rediseño. Se le preguntó al usuario qué hacer y eligió mergear. Se hizo `git merge origin/feature/rediseno-web --no-edit` (fast-forward limpio, sin conflictos) y se corrió `npm run build` para confirmar que compila.
- Archivos tocados: merge trajo cambios en `app/layout.tsx`, `app/globals.css`, `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`, borra `app/components/BottomNav.tsx`, crea `app/components/Navbar.tsx` y `documents/mds/Fupa nuevo/12-rediseno-web-completo.md`. Ninguno editado a mano en este paso, solo mergeado.
- Estado: completo localmente. `main` local quedó actualizado (fast-forward `684ad7c..59e365e`) pero **todavía no se pusheó a `origin/main`** — no se pidió explícitamente. Las 2 ramas restantes (`chore/guardar-config-y-docs`, `chore/workflow-equipo`) no se revisaron todavía.
- Próximo paso sugerido: confirmar con el usuario si hay que pushear este merge a `origin/main`, y si quiere que se revisen las otras 2 ramas `chore/*` pendientes. Recordar (por las entradas 17/19) borrar `.next` antes de levantar `npm run dev` limpio para ver el resultado, ya que hubo commits de build de por medio.

## 21 — Backlog de backend real: autenticación, mascotas y solicitudes conectadas a MySQL (a criterio propio, pedido abierto del usuario)
- Qué se hizo: el usuario pidió trabajar el backlog de AGENTS.md "sabiendo para qué es la app" y usando criterio propio. Se priorizó lo marcado como imprescindible, empezando por autenticación (todo lo demás depende de usuarios reales) y siguiendo por mascotas/solicitudes, que es el objetivo explícito de la fase actual del proyecto.
  - **Schema**: se agregó una migración (`prisma/migrations/20260911234041_auth_real_mascotas_solicitudes`) con `Mascota.razaTexto`/`edadTexto` (texto libre, en vez de forzar ya el catálogo `Raza`/`edadAproxMeses` que todavía no se carga desde ningún lado), `Mascota.mediaUrl`/`mediaType` y `Mascota.adoptanteId` (+ relación inversa en `Adoptante`) para poder registrar la transferencia de "propiedad" al aprobar una adopción.
  - **Autenticación real**: se descartó NextAuth/Auth.js (que recomendaba el backlog) a favor de una sesión propia más liviana — con login solo por email+contraseña (sin proveedores OAuth), el adapter de Prisma de NextAuth no sumaba mucho, y evita una dependencia grande con compatibilidad todavía no probada contra Next 16/React 19. Se implementó: `bcryptjs` para hashear contraseñas, `lib/session.ts` con una cookie httpOnly firmada con HMAC-SHA256 (secreto nuevo `SESSION_SECRET` generado y agregado a `.env`, sin imprimir su valor), y `lib/prisma.ts` como singleton del client. Server actions en `app/actions/auth.ts`: `loginAction`, `registerAdoptanteAction`, `registerProtectoraAction` (crea también la `Protectora` del dueño), `logoutAction`. `AuthContext.tsx` pasó de manejar sesión mock en localStorage a ser un contexto de solo lectura alimentado por `getSession()` en `app/layout.tsx` (ahora Server Component `async`) — ya no tiene `login()`/`logout()`, esos ahora son los server actions. `/login` y `/registro` se reescribieron con `useActionState` para mostrar errores reales (email ya registrado, contraseña incorrecta, contraseña muy corta); se sacó el selector de rol falso de `/login` — el rol sale siempre de la sesión real, nunca de un botón que elige el usuario.
  - **Mascotas y solicitudes reales**: se borraron `app/context/MascotasContext.tsx`, `app/context/SolicitudesContext.tsx`, `data/mock-mascotas.ts` y `data/mock-protectoras.ts` (localStorage) — ya no los importa nada. `/mascotas`, `/mascotas/[id]`, `/protectoras`, `/protectoras/[id]` y la landing (`app/page.tsx`) pasaron a ser Server Components que leen directo de Prisma. `/mascotas/[id]` arma la libreta de vacunación real a partir de la tabla `Vacunacion` (cada fila es una dosis aplicada; si tiene `proximaDosis` se agrega además como pendiente) en vez de un array mock hardcodeado. Se agregaron `app/actions/mascotas.ts` (`addMascotaAction`) y `app/actions/solicitudes.ts` (`crearSolicitudAction`, `actualizarEstadoSolicitudAction`) — esta última, al aprobar, además pone `Mascota.estado = ADOPTADO` y completa `adoptanteId` (la transferencia de "propiedad" que pedía el backlog). `/perfil` se reescribió completo como Server Component (lee sesión + datos con Prisma) con dos client components chicos: `AgregarMascotaForm` (por el preview de archivo) y `AdoptarButton` en `/mascotas/[id]` (por el estado de éxito/error de `useActionState`); aprobar/rechazar y cerrar sesión son formularios server-action directos, sin necesitar cliente.
  - **Permisos server-side (crítico del backlog)**: se agregó `requireRole()` en `lib/session.ts`, que revalida el rol contra la cookie firmada (no contra nada que mande el formulario) en las tres server actions de mutación. `actualizarEstadoSolicitudAction` además verifica que la protectora dueña de la mascota sea la de la sesión actual antes de aprobar/rechazar — no alcanza con tener el rol correcto, tiene que ser *esa* protectora. Esto cierra el punto #1 marcado como crítico en AGENTS.md (rol falsificable editando localStorage a mano): ya no existe ningún localStorage de sesión.
  - **Seed**: se creó `prisma/seed.ts` (`npm run db:seed`, configurado también como `prisma.seed` en `package.json` vía `tsx`) que carga 2 protectoras (FUPA y Huellitas de Punilla, cada una con su usuario admin) y las mismas 4 mascotas de ejemplo que tenían los mocks (Firulais y Michi con vacunas), más un usuario adoptante de demo. Contraseña de todos los usuarios de seed: `happypaws123` (documentada acá, no es un secreto real, es data de demo). Sigue sin ser la data real de FUPA — falta que la ONG la provea.
  - **Bug encontrado y corregido en el camino**: la ficha de `/mascotas/[id]` mostraba el botón/link de "Quiero adoptar" incluso si la mascota ya estaba `ADOPTADO` (se detectó recién al probar el flujo completo con `curl`, entrando por URL directa a una mascota recién adoptada). Se corrigió mostrando un badge "Ya encontró un hogar 🏡" en su lugar, y se agregó el mismo chequeo server-side en `crearSolicitudAction` (rechaza con error si la mascota ya está adoptada) para que no se pueda forzar una solicitud nueva sobre una mascota ya adoptada aunque se salte la UI.
  - De paso: se corrigieron los últimos `<a href>` sueltos que quedaban en la landing (`app/page.tsx`) por `<Link>` de `next/link`, y se agregó link a la ficha de cada protectora en `/protectoras` (antes no eran clickeables pese a que `/protectoras/[id]` ya existía).
- Archivos tocados: `prisma/schema.prisma`, `prisma/migrations/20260911234041_auth_real_mascotas_solicitudes/` (nuevo), `prisma/seed.ts` (nuevo), `.env` (agregado `SESSION_SECRET`, sin exponer su valor), `package.json`/`package-lock.json` (`bcryptjs`, `server-only`, `tsx`), `lib/prisma.ts` (nuevo), `lib/session.ts` (nuevo), `app/actions/auth.ts` (nuevo), `app/actions/mascotas.ts` (nuevo), `app/actions/solicitudes.ts` (nuevo), `app/context/AuthContext.tsx` (reescrito), `app/layout.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/mascotas/[id]/AdoptarButton.tsx` (nuevo), `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx` (reescrito), `app/perfil/AgregarMascotaForm.tsx` (nuevo), `app/page.tsx`, `app/components/HeroCTA.tsx` (nuevo). Borrados: `app/context/MascotasContext.tsx`, `app/context/SolicitudesContext.tsx`, `data/mock-mascotas.ts`, `data/mock-protectoras.ts`. `AGENTS.md` (checklist y backlog actualizados).
- Verificación: `npm run build` compiló sin errores (10 rutas, todas dinámicas ahora — esperable, dependen de cookies/DB). Se probó el flujo completo end-to-end contra el MySQL real levantando un `npm run dev` propio en un puerto libre (3001, para no chocar con el `npm run dev` del usuario que seguía corriendo en el 3000) y simulando el navegador con `curl` (sin JS, usando el fallback progresivo de los server actions de Next — no hay navegador real disponible en este entorno, mismo problema que en entradas 17/19): (1) login como adoptante de seed → cookie de sesión firmada correctamente con `rol: ADOPTANTE` y el nombre real de la DB; (2) login como protectora FUPA → cookie con `rol: ADMIN_PROTECTORA`; (3) `/perfil` de cada uno muestra los datos reales correspondientes; (4) solicitud de adopción real creada para "Firulais" vía `crearSolicitudAction`, aparece como "Pendiente" en el perfil del adoptante; (5) aprobada vía `actualizarEstadoSolicitudAction` desde el perfil de la protectora → pasa a "Aprobada"; (6) confirmado por consulta directa a Prisma que `Firulais` quedó con `estado: ADOPTADO` y `adoptanteId` apuntando a la adoptante correcta (transferencia de "propiedad" funcionando); (7) `Firulais` desapareció de `/mascotas` (excluido por estado `ADOPTADO`), lo que llevó a encontrar y corregir el bug de la ficha individual mencionado arriba. **No se pudo terminar de probar por `curl` el caso de seguridad cruzado** (una protectora distinta a la dueña intentando aprobar una solicitud ajena) porque Next.js en modo dev invalida las referencias de server action entre pedidos (mismo mecanismo de protección, no bug); ese chequeo (`solicitud.mascota.protectora.duenioId !== session.userId`) quedó verificado por lectura de código, no en vivo. Se cerró el server de prueba (puerto 3001) al terminar sin tocar el del usuario (puerto 3000).
- Estado: completo y funcional contra MySQL real, con la salvedad de la verificación cruzada de seguridad mencionada arriba (alta confianza por código, no confirmada en vivo) y sin verificación visual en navegador real (mismo problema de entornos anteriores). El `npm run dev` del propio usuario en el puerto 3000 sigue sirviendo el código VIEJO (mock) — hace falta que lo reinicie (parar el proceso, borrar `.next`, `npm run dev` de nuevo) para ver todo esto andando.
- Pendiente del backlog (no se tocó en esta entrada, para mantener el diff enfocado): storage real de fotos/video (sigue siendo blob URL de sesión), modelo de seguimiento posterior a la adopción, datos reales de FUPA (sigue siendo seed placeholder), y todo el bloque de "Recomendaciones" (filtros, recuperación de contraseña, notificaciones por email, dashboard, favoritos, deploy, términos/privacidad reales, toasts en vez de alert, foto/logo de protectora).
- Próximo paso sugerido: que el usuario reinicie su `npm run dev` (puerto 3000) y pruebe en su propio navegador el flujo completo (registro real, login real con las cuentas de seed o una nueva, agregar mascota como protectora, adoptar como adoptante, aprobar/rechazar) — es la primera vez que hay verificación visual real pendiente sobre TODO este trabajo de backend. Después, decidir con el usuario si se commitea (no se hizo ningún commit todavía, son muchos archivos nuevos/borrados) y qué sigue del backlog.

## 22 — Backlog "Recomendaciones": filtros, dashboard, favoritos, logo de protectora
- Qué se hizo: el usuario pidió seguir con el bloque de "Recomendaciones" de AGENTS.md. Se priorizaron los ítems que no dependen de una decisión externa (proveedor de email, cuenta de deploy) o de contenido que no corresponde inventar (texto legal real):
  - **Filtros de búsqueda en /mascotas**: `app/mascotas/FiltrosMascotas.tsx` (client, lee/escribe `especie`/`tamanio` como query params vía `useRouter`/`useSearchParams`, envuelto en `<Suspense>` en la página igual que `/login`). `app/mascotas/page.tsx` ahora recibe `searchParams` y arma un `Prisma.MascotaWhereInput` con esos filtros. Edad no se filtra: sigue siendo texto libre (`edadTexto`), no hay campo numérico cargado para poder rangear sin inventar una conversión.
  - **Dashboard simple para protectora**: en `/perfil`, sección protectora, se agregaron 4 tiles (mascotas totales, disponibles, solicitudes pendientes, adopciones del mes) calculados en el propio Server Component a partir de los datos que ya se traían (sin queries nuevas para el conteo, solo `.filter()` sobre los arrays de `mascotas`/`solicitudesRecibidas` ya cargados; "adopciones del mes" filtra por `estado: APROBADA` y `updatedAt >= inicio del mes actual`).
  - **Favoritos para adoptantes**: tabla nueva `Favorito` (`adoptanteId` + `mascotaId`, `@@unique` para evitar duplicados) agregada al schema (migración `20260912002251_favoritos_y_logo_protectora`, junto con `Protectora.logoUrl` de abajo). `app/actions/favoritos.ts` (`toggleFavoritoAction`, revalida rol ADOPTANTE server-side). Botón ☆/★ agregado en cada card de `/mascotas` (esquina superior derecha) y en la ficha de `/mascotas/[id]`, y una sección nueva "Mis favoritos" en `/perfil` (adoptante) con link directo a cada ficha.
  - **Foto/logo de protectora**: `Protectora.logoUrl` nuevo en el schema (mismo `db.Text` que `Mascota.mediaUrl`, mismo motivo: guarda blob URL de sesión hasta que haya storage real). `app/actions/protectoras.ts` (`actualizarLogoProtectoraAction`, rol ADMIN_PROTECTORA). `app/perfil/LogoProtectoraForm.tsx` (client, mismo patrón de preview con `URL.createObjectURL()` que ya usaba `AgregarMascotaForm`) agregado a la card "Datos de la protectora". El logo (o un 🏠 de placeholder si no hay) se muestra ahora en `/protectoras`, `/protectoras/[id]` y la sección "Protectoras participantes" de la landing.
  - **Se confirmó sin tocar nada** que ya no queda ningún `alert()` en `app/` (se habían ido solos al reescribir login/registro/perfil con server actions en la entrada 21) — ese ítem del backlog ya estaba resuelto de rebote.
  - **No se hicieron** (requieren una decisión del usuario, no una técnica): recuperación de contraseña y notificación por email (dependen de elegir un proveedor de email), deploy a producción (implica acceso a cuentas/infra del usuario), página de términos/privacidad reales (no hay ningún placeholder vivo en el código para reemplazar, y escribir texto legal "real" no es algo para inventar).
  - **Bloqueo encontrado y resuelto**: después de la migración, `npx prisma generate` falló repetidas veces con `EPERM` porque el `npm run dev` del usuario en el puerto 3000 tenía el motor nativo de Prisma (`query_engine-windows.dll.node`) cargado en memoria. Se le preguntó al usuario en vez de matar el proceso por cuenta propia; paró su servidor, se pudo generar el client, y se dejó un `npm run dev` propio corriendo en el mismo puerto 3000 al final (con el código nuevo) para que lo use directo.
- Archivos tocados: `prisma/schema.prisma`, `prisma/migrations/20260912002251_favoritos_y_logo_protectora/` (nuevo), `app/actions/favoritos.ts` (nuevo), `app/actions/protectoras.ts` (nuevo), `app/mascotas/page.tsx`, `app/mascotas/FiltrosMascotas.tsx` (nuevo), `app/mascotas/[id]/page.tsx`, `app/perfil/page.tsx`, `app/perfil/LogoProtectoraForm.tsx` (nuevo), `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/page.tsx`. `AGENTS.md` (backlog "Recomendaciones" actualizado).
- Verificación: `npm run build` compiló sin errores tras regenerar el Prisma Client. Se probó en vivo contra MySQL real con `npm run dev` en el puerto 3000 (ya libre): filtro `?especie=GATO` devuelve solo Michi/Luna, `?tamanio=GRANDE` devuelve solo Rocky (confirmado por `curl` + `grep` sobre el HTML); favorito agregado a Michi como la adoptante de seed → aparece "★ Michi" en "Mis favoritos" de `/perfil` y el botón de `/mascotas` pasa a "Quitar de favoritos"; logo de protectora actualizado vía `curl` con una URL de prueba → aparece en `/protectoras` → revertido a `null` después con un script chico de Prisma para no dejar una imagen rota en la demo. No se probó el dashboard con más de una adopción real en el mes (solo hay una, la de Firulais de la entrada 21) pero la consulta de conteo es directa y de bajo riesgo.
- Estado: completo y funcional contra MySQL real. Sin verificación visual en navegador real (mismo problema de entorno que en entradas anteriores).
- Pendiente del backlog: storage real de fotos/video, modelo de seguimiento posterior a la adopción, datos reales de FUPA, recuperación de contraseña + notificación por email (requieren decidir proveedor), deploy a producción, términos/privacidad reales (requiere contenido legal real, no inventado).
- Próximo paso sugerido: que el usuario pruebe en su navegador (`localhost:3000`, servidor ya corriendo) los filtros, favoritos, dashboard y logo. Decidir si se commitea todo el trabajo de las entradas 21 y 22 (todavía nada está commiteado).

## 23 — Carrusel de consejos generales en /guias (20 recomendaciones, rotación cada 15s)
- Qué se hizo: el usuario pidió, en la sección "Guías generales" de `/guias`, sumar muchas más recomendaciones de cuidado y que se vayan pasando solas cada ~15 segundos. La sección tenía antes solo 2 tips fijos (alimentación y vacunación) escritos a mano en el JSX. Se creó `data/consejos-generales.json` con 20 consejos (id/title/content) cubriendo alimentación, vacunación, desparasitación, agua, ejercicio, higiene dental, baño/cepillado, uñas/oídos, esterilización, identificación (chip/collar), estimulación mental, socialización, calor, frío, arenero, viajes en auto, adaptación post-adopción, cuidados en la vejez, chequeos veterinarios de rutina y convivencia al sumar una mascota nueva — mismo tono cálido que pide AGENTS.md, nunca alarmista. Se creó `app/guias/ConsejosCarousel.tsx` (client component): muestra un consejo a la vez dentro de la misma card que ya existía (mismo estilo `card border-l-4 border-l-brown-main bg-cream`), avanza solo cada 15000ms con `setInterval`, y suma puntitos clickeables debajo (accesibles, con `aria-label`/`aria-current`) para que el usuario también pueda navegar manualmente sin esperar. Se reutilizó la animación `animate-fade-in-up` ya existente en `globals.css` (con `key={consejo.id}` en la card para que se re-dispare en cada cambio) en vez de crear una transición nueva. `app/guias/page.tsx` importa el JSON y reemplaza los dos `<div>` fijos por `<ConsejosCarousel consejos={consejosGenerales} />`; el resto de la página (recomendaciones por raza vía `getRecommendedGuides`) no se tocó.
- Bug propio encontrado en el camino (no del código nuevo): se corrió `rm -rf .next` con el `npm run dev` de prueba corriendo en paralelo para probar el cambio — mismo problema ya documentado en las entradas 17 y 19 (mezclar borrado de `.next` con un dev server activo lo deja en 500 con `ENOENT`/`MODULE_NOT_FOUND`). Se resolvió parando ese proceso (era el servidor de prueba propio, no el del usuario) y levantando `npm run dev` de nuevo limpio.
- Archivos tocados: `data/consejos-generales.json` (nuevo), `app/guias/ConsejosCarousel.tsx` (nuevo), `app/guias/page.tsx`.
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` levantado se confirmó por `curl` que `/guias` sirve los 20 consejos (20 puntitos con `aria-label="Ver consejo N de 20"` en el HTML) y que el contenido de varios consejos nuevos (ej. "Cepillar los dientes...", "Arenero limpio...", "Viajes seguros...") está presente. La rotación automática en sí (cada 15s) es lógica de cliente estándar (`useState`+`setInterval`) que no se puede verificar por `curl`/HTML estático — queda para que el usuario la vea funcionando en su propio navegador, igual que el resto de la interactividad de esta sesión.
- Estado: completo y funcional. Sin verificación visual en navegador real (mismo problema de entorno que el resto de esta sesión).
- Próximo paso sugerido: que el usuario confirme en su navegador que el carrusel efectivamente avanza solo cada 15 segundos y que el ritmo/cantidad de consejos le resulta bien. Si quiere, se puede ajustar el intervalo (`INTERVALO_MS` en `ConsejosCarousel.tsx`) o sumar más consejos al JSON sin tocar el componente.

## 24 — Sacar la falsa personalización de /guias, segunda sección también como carrusel
- Qué se hizo: el usuario notó que la sección "Recomendado especialmente para tu mascota ✨" de `/guias` no personalizaba nada real — quedaba de cuando existía la mascota virtual (ya eliminada, ver AGENTS.md) y `app/guias/page.tsx` tenía hardcodeado `petData = { breedId: 'labrador-retriever', species: 'dog' }` sin ningún dato real del usuario detrás. Dio a elegir entre dejar 3 guías fijas importantes o convertir también esa sección en carrusel como "Guías generales"; se optó por el carrusel, reutilizando el mismo `ConsejosCarousel` en vez de sumar una tercera forma de mostrar contenido. Se sacó por completo el bloque de recomendación falsa (título condicional "Guías recomendadas para tu perro 🐶"/"Recomendado especialmente para tu mascota ✨", `getRecommendedGuides`, `petData`) de `app/guias/page.tsx`, y se agregó una segunda sección "Guías de salud y bienestar" con `<ConsejosCarousel consejos={guiasSalud} />` usando directamente las 22 guías de `data/guides.json` (displasia, braquicefálico, problemas dentales/cardíacos/renales, alergias, ansiedad por separación, etc.) — mismo contenido de antes, pero mostrado tal cual es (guías de salud generales) en vez de fingir que estaban filtradas para "tu mascota". Como `getRecommendedGuides` se quedó sin ningún llamador en código vivo (confirmado por búsqueda; solo quedaban referencias en `documents/` y en un backup viejo), se borró `lib/recommendations.ts` completo. `data/breeds.json` no se tocó a propósito: hay documentos en `documents/mds/bdd pets/` que lo planean para un futuro selector de raza con autocompletado, no es código muerto todavía, es una feature planificada sin implementar.
- Archivos tocados: `app/guias/page.tsx` (reescrito, más chico). Borrado: `lib/recommendations.ts`.
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` limpio (mismo cuidado de siempre con no mezclar borrado de `.next` y dev server corriendo) se confirmó por `curl` que ya no aparece ningún texto de "Recomendado especialmente"/"Guías recomendadas para tu perro", y que ambas secciones muestran su carrusel completo (20 puntitos en "Guías generales", 22 en "Guías de salud y bienestar").
- Estado: completo y funcional. Sin verificación visual en navegador real (mismo problema de entorno de toda la sesión).
- Próximo paso sugerido: que el usuario confirme el resultado visual en su navegador. Si en algún momento se retoma el selector de raza planeado en `documents/mds/bdd pets/`, ahí sí tendría sentido volver a filtrar "Guías de salud y bienestar" por la raza real del usuario en vez de mostrarlas todas.

## 25 — Fondo interactivo global: huellitas y corazones flotando (canvas, 3 capas de profundidad)
- Qué se hizo: el usuario pidió sumarle vida a la app con un fondo tipo canvas/interactivo. Se le preguntó alcance (landing sola / landing+guías / global) y estilo (huellitas y corazones / blobs de gradiente / constelación de puntos); eligió **global** + **huellitas y corazones flotando**. Se creó `app/components/FloatingPawsBackground.tsx` (client component): un `<canvas>` a pantalla completa (`fixed inset-0`, `pointer-events-none`, `aria-hidden`) que dibuja 16 partículas (huellas de pata y corazones, mitad y mitad al azar) repartidas en 3 capas de profundidad — 6 chicas/lentas/muy tenues atrás, 6 medianas en el medio, 4 grandes/rápidas/algo más visibles adelante — con colores de la paleta (`--brown-mid`, `--gold`, `--brown-main`). Cada partícula sube lentamente y se recicla abajo al salir de pantalla arriba (efecto "burbujas"), con un balanceo lateral suave (seno) para que no suban en línea recta, y reacciona al mouse con parallax sutil (las capas de atrás se mueven menos que las de adelante, reforzando la sensación de profundidad — el "triple efecto" que pedía el usuario). Respeta `prefers-reduced-motion` (si está activado, dibuja un frame fijo sin animar ni escuchar el mouse) y pausa el loop de animación cuando la pestaña no está visible (`visibilitychange`) para no gastar batería de más. Se integró en `app/layout.tsx` (dentro de `<body>`, antes de `<AuthProvider>`) para que aparezca en todas las páginas.
- Decisión de implementación (no trivial): para que el fondo se vea en TODA la app sin tener que tocar el fondo sólido (`bg-[var(--brown-lightest)]`, etc.) de cada página una por una, el canvas se ubica con `z-index: 30` — por encima del contenido normal de cada página (que no tiene z-index propio) pero por debajo del `Navbar` (`z-50` sticky). Al ser `pointer-events-none` y con opacidad muy baja por partícula (0.04 a 0.14), queda como una capa decorativa flotando por encima de todo sin bloquear ningún click ni tapar visualmente el contenido. La alternativa (ponerlo realmente "detrás", con z-index negativo) no funciona sitewide sin volver transparentes los fondos sólidos de cada `<main>` uno por uno, que era un cambio mucho más invasivo para el mismo resultado visual.
- Archivos tocados: `app/components/FloatingPawsBackground.tsx` (nuevo), `app/layout.tsx`.
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` limpio se confirmó por `curl` que el `<canvas>` (con `pointer-events-none` y `z-30`) aparece en el HTML de `/` y `/mascotas`, y que el contenido dependiente de sesión en `/perfil` se sigue sirviendo con normalidad (no quedó nada bloqueado ni oculto detrás del canvas). La animación en sí (movimiento, parallax con el mouse, reciclado de partículas) es lógica de canvas/`requestAnimationFrame` que no se puede verificar por `curl` — queda para que el usuario la vea en su navegador.
- Estado: completo y funcional. Sin verificación visual en navegador real (mismo problema de entorno de toda la sesión) — es la pieza de esta sesión donde más importa esa verificación, al ser 100% visual/animada.
- Próximo paso sugerido: que el usuario abra la app y confirme que el efecto se ve bien (cantidad de partículas, velocidad, que no estorbe la lectura) en varias páginas y en mobile. Si se quiere más o menos presencia, ajustar los conteos/alphas en el array `LAYERS` de `FloatingPawsBackground.tsx` es directo y no requiere tocar el resto del componente.

## 26 — Fondo flotante: 3 rondas de ajuste (visibilidad, movimiento real, y que pase detrás del contenido)
- Qué se hizo, en orden, a partir de feedback del usuario probando en su propio navegador (única forma real de detectar estos problemas, no se veían por `curl`):
  1. **"no se mueven las partículas, y no se ve bien"**: las formas dibujadas a mano en `canvas` (elipses para huella, bezier para corazón) más velocidades muy bajas (5-15px/s) y alphas muy bajos (0.04-0.14) quedaban casi invisibles y el movimiento era imperceptible a simple vista. Se subieron alphas (0.14-0.36) y velocidades (14-46px/s), y se reemplazaron las formas hechas a mano por emojis reales (🐾 🧡 💛) dibujados con `ctx.fillText` — mucho más prolijos y consistentes con el resto de la app, que ya usa 🐾 en varios lados.
  2. **"se ven más, pero siguen quietos, no hay animación"**: el problema real no era visibilidad sino que la animación dependía de un loop propio en JavaScript (`requestAnimationFrame`) que, por algún motivo no confirmable sin acceso a la consola del navegador del usuario (sospecha: `prefers-reduced-motion` activo en su Windows, u otra causa no identificada), nunca llegaba a ejecutarse visiblemente. Se descartó el enfoque de `canvas` + rAF por completo y se reescribió `FloatingPawsBackground.tsx` para que cada partícula sea un `<span>` con una animación CSS pura (`@keyframes float-up` en `globals.css`, con `animation-duration`/`animation-delay` random por partícula) — el movimiento pasa a depender del motor de animaciones del navegador, no de ningún timer propio, mucho más difícil que falle en silencio. El parallax con el mouse se mantuvo pero ahora es solo una variable CSS (`--mx`/`--my`) aplicada vía la propiedad `translate` (independiente de `transform`, que es lo que anima el keyframe) — si el parallax fallara, la subida seguiría andando igual, son independientes. Para `prefers-reduced-motion` se dejó una regla que alarga la duración a 90s en vez de frenar del todo (reduce el movimiento sin eliminarlo).
  3. **"cuando lleguen a los elementos... la animación pase por atrás, no por adelante"**: el contenedor estaba en `z-30` (por delante de todo el contenido normal, ya que ningún `<main>` de la app tiene z-index propio y por eso un elemento `fixed` con z-index explícito pinta por encima de contenido no posicionado, sin importar el orden del DOM). Se bajó a `-z-10` (detrás). Pero eso solo no alcanza: cada página tiene un fondo sólido y opaco en su `<main>` (`bg-[var(--brown-lightest)]`, `bg-[var(--cream)]`, gradientes en landing/login/registro, footer) que taparía completamente el fondo flotante en cualquier parte de la página que no sea texto/cards. Se le agregó `/95` (95% opacidad) a esos fondos sólidos en los 7 archivos que usan `bg-[var(--brown-lightest)]` como fondo de página (`app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx` —sus 2 `<main>`—, `app/guias/page.tsx`), a la sección `bg-[var(--cream)]` de la landing, al gradiente de `/login` y `/registro`, y al footer de `app/layout.tsx`. Con eso, el fondo asoma apenas por los huecos de fondo de cada página, mientras que el texto y las cards (que se dibujan en el flujo normal del documento, por delante de cualquier elemento posicionado con z-index negativo) siguen totalmente nítidos y sin que nada pase por encima — exactamente el efecto pedido.
- Archivos tocados: `app/components/FloatingPawsBackground.tsx` (reescrito), `app/globals.css` (nuevo `@keyframes float-up` + `.floating-paw`), `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, `app/layout.tsx`.
- Verificación: `npm run build` compiló sin errores en cada una de las 3 rondas. Por `curl` se confirmó que el contenedor del fondo (con la clase `-z-10` en la última ronda) sigue presente en el HTML de varias páginas, y que las clases `.../95` de transparencia llegaron a los archivos correctos. **Ninguna de las 3 rondas se pudo verificar visualmente en un navegador real** (limitación de entorno de toda la sesión) — el usuario fue quien detectó los primeros 2 problemas probando en el suyo; esta 3ra ronda (pasar detrás del contenido) queda pendiente de que la confirme también.
- Estado: implementado con alta confianza en la lógica (movimiento ahora es CSS puro, mucho más difícil que falle en silencio que el rAF anterior), pero sin confirmación visual final de esta última ronda.
- Próximo paso sugerido: que el usuario recargue (Ctrl+Shift+R) y confirme que ahora el fondo efectivamente pasa detrás de las cards/texto y solo se ve en los huecos entre contenido. Si el 95% de opacidad deja el fondo demasiado tenue o demasiado presente, es un ajuste directo (cambiar `/95` por otro valor) en los mismos archivos.

## 27 — El fondo con `-z-10` desapareció del todo: se cambió a stacking explícito entre hermanos
- Qué se hizo: el usuario reportó que, después del cambio de la entrada 26 (bajar el fondo a `-z-10`), la animación directamente desapareció por completo (ni siquiera asomaba en los huecos entre contenido). El razonamiento de la entrada 26 asumía que, al no tener `<body>`/`<html>` su propio contexto de apilamiento, un z-index negativo en un elemento `fixed` quedaría ordenado correctamente respecto al contenido normal del documento — en la práctica no se comportó así (no se pudo confirmar la causa exacta sin acceso a devtools del navegador del usuario). Para sacarse de encima esa ambigüedad, se cambió a un patrón de apilamiento explícito y sin dudas: en `app/layout.tsx`, todo el contenido de la app (`<Navbar/>{children}<footer/>`) se envolvió en un `<div className="relative z-10">` (adentro de `AuthProvider`, así el hook `useAuth` sigue disponible), y `FloatingPawsBackground` pasó de `-z-10` a `z-0` (positivo, no negativo). Ahora la comparación es directa entre dos hermanos con posición explícita (`fixed`/`relative`) y z-index numérico (`0` contra `10`) en el mismo contexto de apilamiento — el caso más simple y predecible de las reglas de stacking de CSS, sin depender de si `body`/`html` arman o no su propio contexto. El resto de la entrada 26 (fondos de página al 95% de opacidad para que el fondo asome en los huecos) se mantuvo sin cambios.
- Archivos tocados: `app/layout.tsx` (agregado el wrapper `relative z-10`), `app/components/FloatingPawsBackground.tsx` (`-z-10` → `z-0`).
- Verificación: `npm run build` compiló sin errores. Por `curl` se confirmó que el HTML tiene exactamente esa estructura: el `<canvas>`... perdón, el contenedor de partículas con `z-0`, y el `<div class="relative z-10">` envolviendo el resto. Sigue sin poder verificarse visualmente en un navegador real desde este entorno — cuarta ronda de ajuste de este mismo efecto basada 100% en el feedback visual del usuario, que es quien tiene que confirmar si ahora sí funciona.
- Estado: implementado con el patrón de stacking más simple y menos ambiguo posible; pendiente de confirmación visual real (van 2 rondas seguidas donde el ajuste de z-index no dio el resultado esperado a la primera, así que esta vez no se da por buena hasta que el usuario la vea).
- Próximo paso sugerido: que el usuario recargue y confirme. Si TODAVÍA no aparece nada, el problema probablemente no es de z-index sino de que el propio `<div>` de partículas nunca llega a tener contenido (revisar en devtools si `.floating-paw` existe en el DOM inspeccionado, no solo en el HTML servido) — en ese caso el debugging tendría que hacerse mirando la consola/inspector del navegador del usuario directamente, algo que no es posible desde esta sesión.

## 28 — Regresión con el wrapper `relative z-10`: revertido al último estado que andaba bien
- Qué se hizo: el cambio de la entrada 27 (envolver todo el contenido en `<div className="relative z-10">` dentro de `app/layout.tsx`, más `FloatingPawsBackground` en `z-0`) resultó en una regresión real reportada por el usuario ("quedó peor, desaparecieron cosas, las animaciones ya no se ven") — no una mejora parcial, sino algo roto. Se intentó primero conectar la extensión de Chrome (`claude-in-chrome`) para poder ver la página real y depurar con la consola del navegador en vez de seguir a ciegas por `curl`, pero el usuario todavía no tiene la extensión conectada en esta sesión (la instaló a mitad de conversación, pero la conexión solo se completa en una sesión nueva de Claude Code, no en esta). Sin esa herramienta y sin poder confirmar la causa exacta de la regresión (no hay errores en el log del servidor — es 100% un problema de cliente/CSS), se decidió no seguir ajustando a ciegas y en su lugar revertir por completo al último estado confirmado como funcional por el usuario (mensaje anterior: "ahora si quedo, pero..."): `FloatingPawsBackground` de vuelta a `z-30` (adelante de todo el contenido, como estaba cuando el usuario confirmó que el movimiento se veía bien), se sacó el `<div className="relative z-10">` de `app/layout.tsx`, y se revirtieron las 14 apariciones de `/95` (transparencia) agregadas en la entrada 26 a los fondos de página (`app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`, footer de `app/layout.tsx`) — se dejó sin tocar el único `/95` que ya existía de antes en `Navbar.tsx` (no era mío).
- Archivos tocados: `app/layout.tsx`, `app/components/FloatingPawsBackground.tsx`, `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`.
- Verificación: `npm run build` compiló sin errores. Por `curl` se confirmó que `FloatingPawsBackground` volvió a `z-30` y que ningún fondo de página tiene ya `/95` (solo queda el `/95` preexistente de `Navbar.tsx`, sin tocar). Esto reproduce textualmente el HTML que el usuario había confirmado como funcional (movimiento visible) antes de pedir que pasara "por atrás".
- Estado: revertido a un estado conocido y confirmado por el usuario. El pedido original de esta sub-tarea ("que pase por atrás de los elementos") queda SIN resolver — se intentó 2 veces (entradas 26 y 27) sin éxito y con una regresión de por medio, así que no se va a seguir intentando a ciegas.
- Próximo paso sugerido: esperar a que el usuario complete la conexión de la extensión de Chrome (instalada, falta completar la conexión — el propio sistema indica que se detecta sola en la próxima sesión de Claude Code, o corriendo `/chrome`) para poder depurar esto viendo la página real y la consola, en vez de seguir adivinando por `curl`. Mientras tanto, el fondo animado queda funcionando pero por DELANTE del contenido (última versión confirmada como visualmente aceptable, aunque no ideal).

## 29 — Prompt 13 (mascota animada en /login: sigue el mouse, se tapa los ojos)
- Qué se hizo: el usuario subió `documents/mds/Fupa nuevo/13-mascota-login-animada.md`, un prompt con código ya provisto (no había que diseñar nada, solo aplicarlo). Se ejecutaron las 3 tareas tal cual: (1) `app/components/LoginMascot.tsx` (nuevo, "use client"), un perrito en SVG cuyos ojos siguen el mouse (offset máximo 4px, calculado a partir de la posición del cursor relativa al centro del componente) y cuyas patitas bajan a taparle los ojos cuando `isPasswordFocused && !isPasswordVisible`. (2) En `app/(auth)/login/page.tsx` se agregó el toggle de "mostrar contraseña": estado `showPassword`/`passwordFocused`, íconos `Eye`/`EyeOff` de `lucide-react` (ya era dependencia del proyecto), el input de contraseña pasó a `type={showPassword ? "text" : "password"}` con `onFocus`/`onBlur` para trackear el foco, envuelto en un `div relative` con el botón de toggle en `absolute right-3` y `pr-10` en el input para que no se superpongan. (3) Se importó y renderizó `<LoginMascot isPasswordFocused={passwordFocused} isPasswordVisible={showPassword} />` arriba del `<h1>Iniciar sesión</h1>`, sin tocar nada de la lógica de login real (server action `loginAction`, `useActionState`, `redirectTo`, mensaje de error) — es 100% agregado visual sobre el formulario que ya funcionaba. Tal como pedía el prompt, no se tocó `/registro`.
- Archivos tocados: `app/components/LoginMascot.tsx` (nuevo), `app/(auth)/login/page.tsx`.
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` limpio se confirmó por `curl` que `/login` sirve el SVG de 120x120 de la mascota y el botón con `aria-label="Mostrar contraseña"` (estado inicial correcto, input en `type="password"`). Se hizo además una prueba funcional completa de login (con las mismas credenciales de seed que se vienen usando en toda la sesión) para confirmar que agregar la mascota y el toggle no rompió nada de la lógica real: sesión creada correctamente, cookie firmada con el rol y nombre reales de la base. El seguimiento del mouse con los ojos y la animación de las patitas tapando/destapando son interacción pura de cliente (`useState`/`mousemove`/transiciones CSS) que no se puede confirmar por `curl` — queda para que el usuario la vea en su navegador, tal como pedía el prompt original ("Probá manualmente...").
- Estado: completo y funcional a nivel de código, build y lógica de login (confirmado). Verificación visual/interactiva (ojos siguiendo el mouse, patitas tapando al enfocar contraseña, destapando con "mostrar contraseña") pendiente del usuario.
- Próximo paso sugerido: que el usuario pruebe en `/login`: mover el mouse y ver si los ojos lo siguen (sutil, 4px máximo — el propio prompt aclara que si se ve muy poco/mucho se ajusta `maxOffset` en `LoginMascot.tsx`), hacer foco en el campo de contraseña y confirmar que las patitas bajan, y activar "mostrar contraseña" para confirmar que se destapan. Si en algún momento se quiere el mismo componente en `/registro`, el prompt aclara que se puede reusar tal cual en un pedido aparte.

## 30 — Ajuste de LoginMascot: la cabeza también gira, y las patas suben desde abajo
- Qué se hizo: el usuario pidió dos ajustes sobre `LoginMascot.tsx` (entrada 29) después de probarlo: (1) que la cabeza también se mueva siguiendo el mouse, no solo las pupilas; (2) las patitas se veían mal porque a estado de reposo quedaban asomando por ARRIBA de la cabeza (el group original hacía `translateY(-55px)` para "esconderlas", pero solo las corría hacia la zona de las orejas en vez de ocultarlas bien) — pidió que en vez de eso descansen abajo y la animación de taparse suceda subiendo desde ahí. (1) Se agrupó orejas+cara+nariz+boca en un único `<g>` que rota levemente (`rotate(headTilt)` con `headTilt = eyeOffset.x * 1.5`, máximo ~6°) y se desplaza un poco en Y (`headLift = eyeOffset.y * 1.2`) seteando `transformOrigin: "60px 60px"` para que pivotee desde el centro de la cara — reutiliza el mismo `eyeOffset` que ya se calculaba para las pupilas, no hizo falta un cálculo nuevo. Los ojos quedaron en su propio `<g>` separado (como ya estaban) para que las pupilas se sigan moviendo con su propio offset independiente de la rotación de la cabeza. (2) El offset de reposo de las patitas pasó de `translateY(-55px)` (arriba, mal escondidas cerca de las orejas) a `translateY(48px)` (abajo, cerca del mentón/debajo de la boca, sin superponerse con nariz/boca) — la posición de "tapando" se mantuvo en `translateY(0px)` (mismo Y que los ojos), así que ahora la animación es: patitas descansando abajo → suben 48px para tapar los ojos, en vez de bajar desde arriba.
- Archivos tocados: `app/components/LoginMascot.tsx`.
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` limpio se confirmó por `curl` que `/login` sigue sirviendo 200. El resultado visual (que la cabeza gire de forma sutil y prolija, y que las patitas ahora se vean bien en reposo abajo sin pisar la boca) es 100% interacción/CSS de cliente — no se puede confirmar por `curl`, queda para que el usuario lo vea en su navegador.
- Estado: completo a nivel de código y build; verificación visual pendiente del usuario (mismo patrón que toda la sesión).
- Próximo paso sugerido: que el usuario pruebe en `/login` y confirme si el giro de cabeza y la nueva posición de las patitas se ven bien, o si hay que ajustar los valores (`headTilt`/`headLift`/el `translateY(48px)` de reposo) — son números a mano, fáciles de afinar con feedback visual real.

## 31 — LoginMascot: la cabeza tiene que seguir al mouse de verdad, no solo un tilt sutil
- Qué se hizo: el usuario aclaró que el pedido de la entrada 30 no era un tilt apenas perceptible sino que la cabeza siga al mouse de forma clara, como ya hacían los ojos. El `headTilt = eyeOffset.x * 1.5` (máximo ~6°) reutilizaba el mismo offset chico calculado para las pupilas (pensado para moverse apenas unos px dentro del ojo), así que el movimiento de cabeza quedaba casi invisible. Se separó el cálculo en dos magnitudes a partir de una misma dirección de mouse normalizada (`mouse.x`/`mouse.y`, -1..1, con un radio de 220px en vez de 100px para que la cabeza siga respondiendo con el cursor más lejos): la cabeza ahora rota hasta ±12° y se desplaza hasta 6px en X / 5px en Y (`headTilt`/`headShiftX`/`headShiftY`), mientras que las pupilas usan un offset más chico (`pupilOffset`, máximo 4px, mismo criterio sutil que pedía el prompt original para los ojos). Se aprovechó para meter los ojos DENTRO del mismo `<g>` que rota con la cabeza (antes eran un grupo hermano aparte) — así ojos, orejas, cara, nariz y boca giran todos juntos como una sola cabeza, y las pupilas suman su propio movimiento más fino encima. Se agregó `overflow: "visible"` al `<svg>` para que las orejas no se corten contra el borde del viewBox cuando la cabeza rota/desplaza hacia los costados.
- Archivos tocados: `app/components/LoginMascot.tsx` (reescrito).
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` limpio se confirmó por `curl` que `/login` sigue sirviendo 200. El movimiento en sí (que la cabeza gire/traslade de forma clara siguiendo el cursor) es interacción de cliente que no se puede confirmar por `curl` — queda para el usuario.
- Estado: completo a nivel de código y build; verificación visual pendiente.
- Próximo paso sugerido: que el usuario confirme si ahora el movimiento de cabeza es lo bastante notorio (o si hay que subir/bajar los grados de rotación o el desplazamiento en px de `headTilt`/`headShiftX`/`headShiftY`).

## 32 — Segundo intento de mandar el fondo atrás (esta vez sin wrapper nuevo) + más partículas
- Qué se hizo: el usuario pidió sumar más huellitas/corazones (le parecían pocas) y volver a mandar el fondo atrás del contenido. Se subieron los conteos de `LAYERS` en `FloatingPawsBackground.tsx` (6/6/5 → 10/10/8, total 17 → 28 partículas) y se volvió a bajar el contenedor a `z-0`. Para el "atrás" se evitó repetir el approach de la entrada 27 (que agregaba un `<div className="relative z-10">` nuevo envolviendo `Navbar+children+footer` y causó la regresión de la entrada 28) — en cambio, como `AuthProvider` no agrega ningún nodo DOM propio (es solo un `Context.Provider`), `Navbar`, el `<main>` de cada página y el `<footer>` ya son hermanos directos del fondo flotante en el DOM real. Entonces se agregó `relative z-10` directo a los elementos que YA EXISTÍAN (sin crear ningún div nuevo): el `<main>` de las 9 páginas (`app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx` —sus 2 `<main>`—, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx` —sus 2—, `app/perfil/page.tsx` —sus 2—, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`) y el `<footer>` de `app/layout.tsx`. `Navbar` no necesitó cambios: ya tiene `sticky` + `z-50` propio, así que ya gana la comparación de stacking sin tocar nada. Se re-agregó el `/95` de opacidad a esos mismos fondos (y a la sección crema de la landing) para que el fondo asome en los huecos, igual que en el intento de la entrada 26. Se revisó antes de aplicar que ningún elemento `absolute` de la app dependa de que su `<main>` sea `position: static` (todos los `absolute` existentes ya cuelgan de su propio contenedor `relative` más cercano — el hero, las cards de imagen, el label de contraseña —, ninguno apunta a `<main>` directamente), así que agregarle `position: relative` no debería romper ningún posicionamiento existente.
- Archivos tocados: `app/components/FloatingPawsBackground.tsx`, `app/layout.tsx`, `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx`.
- Verificación: `npm run build` compiló sin errores. Con `npm run dev` limpio se confirmó por `curl` que las 6 rutas principales devuelven 200, que el contenedor del fondo tiene `z-0`, que los `<main>` tienen `relative z-10` + `/95`, y que el contenido real de las páginas se sigue sirviendo sin problemas (mascotas listadas, `/perfil` mostrando "Cuenta de adoptante" con la sesión real). No se pudo verificar visualmente si el fondo efectivamente asoma detrás del contenido ahora — es la tercera vez que se intenta esto y las dos anteriores no salieron bien a la primera, así que esta vez no se da por resuelto hasta que el usuario lo confirme.
- Estado: implementado de la forma más quirúrgica posible (sin agregar nodos DOM nuevos, solo clases sobre elementos existentes), con más partículas. Pendiente de confirmación visual — si vuelve a romper algo, revertir apuntando a esta entrada (los cambios están acotados a las clases `relative z-10` y `/95` listadas arriba, más el `z-0` del fondo).
- Próximo paso sugerido: que el usuario confirme (1) que ahora sí ve más partículas y (2) que el fondo pasa detrás del contenido sin que nada se haya roto. Si algo desaparece de nuevo, avisar QUÉ elemento puntual desapareció (no solo "algo desapareció") para poder aislar la causa más rápido la próxima vez.

## 33 — Causa real de por qué "atrás" nunca se vio: matemática de opacidad, no z-index. Vuelta a "adelante" definitiva
- Qué se hizo: el usuario reportó que con el cambio de la entrada 32 ya no se veían las huellas en absoluto (esta vez sin romper nada más, solo invisibles). Se identificó la causa real, que explica por qué las 2 entradas anteriores (26 y 32) fallaron en lo mismo aunque con mecanismos de z-index distintos: la opacidad de fondo (`/95`, 95% opaco) y la opacidad de cada partícula se MULTIPLICAN, no se suman. Una huellita con opacidad ~0.3 vista a través de un fondo 95% opaco (solo 5% transparente) queda en 0.3 × 0.05 ≈ 0.015 de opacidad real — invisible en la práctica. Para que "atrás" funcionara de verdad hacía falta bajar la opacidad del fondo de cada página a ~40-60% (no ~95%), lo cual cambia notoriamente el tono crema/marrón de cada página (deja de ser un ajuste sutil). Se le explicó esto al usuario con la cuenta hecha y se le preguntó cómo seguir; eligió dejarlo adelante en vez de aceptar ese cambio de tono más fuerte. Se revirtieron todos los `relative z-10` y `/95` agregados en la entrada 32 (mismos 11 archivos) y se volvió a poner `FloatingPawsBackground` en `z-30` (adelante) — pero conservando el aumento de partículas de esa misma entrada (17 → 28, los conteos de `LAYERS` no se tocaron).
- Archivos tocados: `app/components/FloatingPawsBackground.tsx` (`z-0` → `z-30`, conteos de `LAYERS` sin cambios respecto a la entrada 32), `app/layout.tsx`, `app/page.tsx`, `app/mascotas/page.tsx`, `app/mascotas/[id]/page.tsx`, `app/protectoras/page.tsx`, `app/protectoras/[id]/page.tsx`, `app/perfil/page.tsx`, `app/guias/page.tsx`, `app/(auth)/login/page.tsx`, `app/(auth)/registro/page.tsx` (revertidos a sin `relative z-10` ni `/95`).
- Verificación: `npm run build` compiló sin errores. Por `curl` se confirmó que no queda ningún `relative z-10`/`/95` agregado por la entrada 32 (búsqueda en todo `app/`, aparte del `/95` preexistente de `Navbar.tsx`) y que el fondo volvió a `z-30`.
- Estado: el fondo animado queda definitivamente por DELANTE del contenido (con 28 partículas), que es el estado que el usuario confirmó como bueno tanto acá como en la entrada 25. El pedido de "mandarlo atrás" queda descartado por decisión del usuario, no pendiente — no tiene sentido seguir intentándolo salvo que el usuario en algún momento acepte el cambio de tono de fondo más fuerte que hace falta para que funcione.
- Próximo paso sugerido: ninguno pendiente sobre este tema. Si en el futuro se quiere retomar "atrás", la única forma de que funcione es aceptar bajar la opacidad de fondo de cada página a ~40-60% (no un ajuste sutil).


## 34 — Proyecto movido de C: a D:
- Qué se hizo: el usuario pidió mover todo el proyecto al disco D: sin romper nada. Antes de mover se revisó `git status` (había mucho trabajo sin commitear de toda la sesión — entradas 21 a 33 — así que el traslado se hizo con cuidado extra) y se paró el `npm run dev` que había quedado corriendo en el puerto 3000 (para soltar el lock del motor de Prisma, mismo problema visto en la entrada 22). Se preguntó al usuario el path exacto de destino; eligió mantener la misma estructura relativa, solo cambiando de unidad: `D:\Facultad\practicas profesionalizantes\happy-paws`. Se copió con `robocopy /E /XD node_modules .next` (excluyendo esas dos carpetas a propósito — 881MB y 81MB respectivamente, se regeneran solas y copiarlas no aportaba nada, solo tiempo/riesgo). En la carpeta nueva se corrió `npm install`, `npx prisma generate` y `npm run build` para confirmar que todo funciona igual. Se verificó que `.env` se copió correctamente (contiene `DATABASE_URL` apuntando a `localhost:3306`, no depende de la carpeta del proyecto) y que `git status`/`git remote -v`/`git log` en la carpeta nueva muestran exactamente lo mismo que en la vieja (mismo remoto, mismos commits, mismos cambios sin commitear — nada se perdió en la copia). Se levantó `npm run dev` desde la carpeta nueva y respondió 200 con datos reales de MySQL.
- Archivos tocados: ninguno de código — es una copia completa del proyecto a `D:\Facultad\practicas profesionalizantes\happy-paws`. La carpeta original en `C:\Users\Tinu\Desktop\Facultad\practicas profesionalizantes\happy-paws` **todavía no se borró** — se dejó intacta a propósito hasta que el usuario confirme que todo anda bien en D: y pida explícitamente borrarla (mover con copia+verificación+borrado manual es más seguro que un "mover" directo cuando hay tanto trabajo sin commitear de por medio).
- Verificación: `npm install`, `npx prisma generate` y `npm run build` corrieron sin errores en la ubicación nueva. `git status`/`git remote -v`/`git log -3` idénticos entre C: y D:. `npm run dev` desde D: respondió 200 en `/` y `/mascotas`, con nombres reales de mascotas de la base (confirma que la conexión a MySQL no se rompió con el cambio de carpeta).
- Estado: completo y verificado. **A partir de ahora, cualquier trabajo nuevo de esta sesión debería hacerse sobre `D:\Facultad\practicas profesionalizantes\happy-paws`, no sobre la copia vieja en C:**, para no terminar con dos copias divergentes.
- Próximo paso sugerido: que el usuario confirme que todo funciona abriendo el proyecto en D: con su editor/terminal habitual, y que decida si borrar la copia vieja de C: (no se borró sola a propósito).

## 35 — Push de todos los cambios pendientes a GitHub
- Qué se hizo: el usuario pidió pushear todo. Se revisó `git status` (había cambios sin commitear de toda la migración a backend real, más 3 commits locales previos ya adelantados a `origin/main`) y se confirmó que no hay ningún archivo `.env`/secreto entre lo modificado o sin trackear (está correctamente ignorado por `.gitignore`, `lib/session.ts` usa `process.env.SESSION_SECRET`, no un valor hardcodeado). Se stageó todo (excluyendo explícitamente `.env`), se hizo un commit único describiendo la migración de AuthContext/MascotasContext/SolicitudesContext mock a Prisma+MySQL con permisos server-side, favoritos y logo de protectora, y se pusheó a `origin/main`.
- Archivos tocados: ninguno de código nuevo — commit y push de lo que ya estaba en el working tree (ver commit `3591742`).
- Estado: completo. `origin/main` en GitHub quedó actualizado (`684ad7c..3591742`), incluye los 3 commits previos que estaban solo en local más este nuevo commit.
- Próximo paso sugerido: ninguno pendiente sobre este pedido puntual. Los ítems abiertos del backlog en `AGENTS.md` (storage real de fotos/video, recuperación de contraseña, seguimiento post-adopción, deploy a producción, términos/privacidad) siguen igual que antes.
